name: build_ErfanGSIs

on:
#  release:
#    types: [published]
#  push:
#    branches:
#      - 
#    paths:
#      - '.config'
#  schedule:
#    - cron: 0 8 * * 5
  watch:
    types: [started]
    
env:
  ROM_URL: https://cloud189-henan-person.oos-hazz.ctyunapi.cn/PERSONCLOUD/0fbc99c9-e2e4-4e40-b6e2-f4e1cf7caf4b.zip?x-amz-CLIENTTYPEIN=UNKNOWN&AWSAccessKeyId=532482dfdc85e5d60592&x-amz-UID=755338272&x-amz-APPID=828221&response-content-disposition=attachment%3Bfilename%3D%22MICC9_MIUI12_20.12.9_10.0_ZhuiMeng_7883e0b660.zip%22&x-amz-CLIENTNETWORK=UNKNOWN&x-amz-CLOUDTYPEIN=PERSON&x-amz-limit=rate%3D10240%2Cconcurrency%3D15&Signature=l4JMdrPHuviGC9ZUFrbyYGQDsEc%3D&x-amz-SHID=170961408&Expires=1610575017&x-amz-FSIZE=2384910813&x-amz-UFID=51450112273397294
  ROM_NAME: MIUI
  TZ: Asia/Shanghai


jobs:
  build:
    runs-on: ubuntu-latest
    if: github.event.repository.owner.id == github.event.sender.id

    steps:
       - name: Checkout
         uses: actions/checkout@master
         
       - name: Cleaning up
         run: |
           docker rmi `docker images -q`

       - name: Cloning ErfanGSIs tools
         run: |
            git clone --recurse-submodules https://github.com/WhiteMouseHuang/ErfanGSIs.git
            sudo chmod -R 777 ErfanGSIs
            curl -sL https://git.io/file-transfer | sh
             
       - name: Setting up Porting Environment
         run: |
           cd ErfanGSIs
           sudo apt update
           sudo bash setup.sh
       
       - name: Download Stock Rom & Generate GSI
         run: |
           sudo ./ErfanGSIs/url2GSI.sh $ROM_URL $ROM_NAME --ab
           ls /home/runner/work/actions_build_ErfanGSIs/actions_build_ErfanGSIs/ErfanGSIs/output/
           7z a -r $ROM_NAME-ab.7z /home/runner/work/actions_build_ErfanGSIs/actions_build_ErfanGSIs/ErfanGSIs/output/$ROM_NAME-AB-10-*-ErfanGSI.img
           sudo ./transfer wet $ROM_NAME-ab.7z
#           sudo rm $ROM_NAME-ab.7z
#           7z a -r $ROM_NAME-aonly.7z /home/runner/work/actions_build_ErfanGSIs/actions_build_ErfanGSIs/ErfanGSIs/output/$ROM_NAME-Aonly-*-ErfanGSI.img
#           sudo ./transfer wet $ROM_NAME-aonly.7z
